# 接口协议 v0.1（草稿 · 待冻结）

> **状态：草稿** —— 冻结后本文件**只增不改**（废弃字段标 `deprecated`，不删除、不改名，版本号 +1）。
> **维护人：成员 C**。任何字段改动需 A 或 B 至少一方确认，并在此留下变更记录。
>
> 为什么这个文件最重要：三人并行开发 = 两个人在没有对方代码的情况下也要能写。**契约是唯一的粘合剂。**

---

## 一、角色与方向

| 角色 | 是谁 | 做什么 |
|---|---|---|
| 后端（服务方） | 成员 A | 姿态/行为/手机数据汇总 → 通过 WebSocket 推给前端；对外提供 HTTP 接入 |
| 前端（消费方） | 成员 B | 消费 WebSocket 消息，渲染房间与伙伴；**只读，不反向控制**（v0.1 不做） |
| 数据源（外部） | 成员 C | 手机（快捷指令 / HTTP Shortcuts）通过 HTTP 上报；Windows 行为采集在本地进程内 |

```
手机 ──HTTP POST /api/ingest/health──┐
摄像头+Windows ──本地进程──▶ 后端 A ──WebSocket /ws──▶ 前端 B
```

---

## 二、通用约定

| 项 | 约定 |
|---|---|
| 协议版本 | 每条消息带 `v`（整数）。v0 系列内**只增字段**，前端必须忽略未知字段 |
| 时间戳 | 一律为 **Unix 毫秒**（`1757990000123`），未知时用 `null`，不要用 0 |
| 命名 | **snake_case**（`neck_angle_deg`） |
| 状态值 | 全小写英文枚举，见下 |
| 推送频率 | 后端推送 ≤ **1 条/秒**（状态变化时才推；心跳另计） |
| 数值单位 | 角度 `deg`、时长 `sec`、距离 `cm`、步数 `步` |

### 姿态状态枚举 `pose_state`（**已确认，改动必须三人同意**）

| 值 | 中文 | 含义 |
|---|---|---|
| `unknown` | 未知 | 未检测到人 / 置信度过低 |
| `upright` | 坐直 | 正常良好坐姿 |
| `slouch` | 驼背 | 头前倾 + 躯干后靠 |
| `desk_hunch` | 伏案 | 躯干前倾逼近桌面 |
| `away` | 离开 | 座位上没有人 |
| `drinking` | 喝水 | 抬臂举杯（**v0.1 可选**，识别不稳就先不做） |

### 行为等级枚举 `behavior_level`

| 值 | 含义 |
|---|---|
| `ok` | 正常 |
| `warn` | 持续偏长，房间环境给出轻提示 |
| `alert` | 超过阈值，触发角色提醒 |

---

## 三、WebSocket `/ws`

连接：`ws://<后端地址>:8000/ws`（无鉴权，v0.1 仅局域网使用）

### 3.1 `hello`（连接建立后**第一帧**，全量快照）

```json
{
  "v": 1,
  "type": "hello",
  "ts": 1757990000123,
  "server": { "version": "0.1.0", "capabilities": ["pose", "behavior", "phone"] },
  "pose": { "state": "upright", "confidence": 0.91, "since_ts": 1757989990000 },
  "behavior": { "active_app": "Code.exe", "session_sec": 1830, "level": "ok" },
  "phone": { "steps": 3210, "updated_ts": 1757989000000, "stale": false }
}
```

### 3.2 `state`（增量推送，只带**发生变化**的顶层字段）

```json
{
  "v": 1,
  "type": "state",
  "ts": 1757990010000,
  "pose": {
    "state": "slouch",
    "confidence": 0.87,
    "since_ts": 1757990008000,
    "sustained_sec": 2.0,
    "metrics": { "neck_angle_deg": 42.3, "shoulder_tilt_deg": 3.1, "distance_cm": 62 }
  }
}
```

- `metrics` 仅用于调试与可视化，**前端不得依赖它做逻辑判断**（字段可能缺失）。
- `sustained_sec` = 当前状态已持续时长，前端做过渡动画的时间基准。

### 3.3 `reminder`（提醒事件）

```json
{ "v": 1, "type": "reminder", "ts": 1757993600000, "kind": "water", "level": "alert" }
```

`kind` 取值：`water`（喝水）｜`standup`（久坐起身）。

### 3.4 `ping` / `error`

```json
{ "v": 1, "type": "ping", "ts": 1757990010000 }
{ "v": 1, "type": "error", "ts": 1757990010000, "code": "POSE_LOST", "message": "摄像头断流" }
```

前端收到 `ping` 忽略即可；连续 **10 秒**无任何消息视为断线，自动重连（间隔 2s 递增，最长 30s）。

---

## 四、HTTP 接入 `POST /api/ingest/health`

**用途**：手机侧零开发上报（iOS 快捷指令 / Android HTTP Shortcuts）。

| 项 | 约定 |
|---|---|
| 路径 | `/api/ingest/health` |
| 鉴权 | Header `X-Xiaoqi-Token: <配对令牌>`（后端首次启动生成，打印在控制台并显示二维码） |
| 监听 | `0.0.0.0:8000`（手机与电脑需在同一 Wi-Fi） |
| 内容类型 | `application/json` |

### 请求体

```json
{ "steps": 3210, "ts": 1757990000000, "source": "ios-shortcut", "device_id": "iphone-zhang" }
```

| 字段 | 必填 | 说明 |
|---|---|---|
| `steps` | ✅ | 当日累计步数（整数，聚合标量） |
| `ts` | ➖ | 采集时间；缺省时用服务端接收时间 |
| `source` | ➖ | `ios-shortcut` / `android-http-shortcuts` / `manual` |
| `device_id` | ➖ | 用于幂等去重的设备标识，缺省时按 IP 处理 |

### 响应

| 码 | 场景 | 体 |
|---|---|---|
| 200 | 接收成功 | `{ "ok": true, "deduped": false }` |
| 200 | 命中幂等（60 秒窗口内同 `device_id` 重复上报） | `{ "ok": true, "deduped": true }` |
| 400 | 字段缺失/类型错 | `{ "ok": false, "error": "INVALID_FIELD", "field": "steps" }` |
| 401 | 令牌错误 | `{ "ok": false, "error": "BAD_TOKEN" }` |

### 规则

1. **幂等**：同一 `device_id` 在 **60 秒**窗口内的重复上报直接丢弃（`deduped: true`），不覆盖已有值。
2. **只前进不回退**：`steps` 是当日累计值，若新值小于已存值（跨零点或系统重置），视为**新的一天**开新记录，不修正旧值。
3. **静止时长**由步数序列差分推断，**不额外申请权限**：连续 N 分钟步数无变化 → 计为静止。
4. iOS 定时自动化需要解锁才可靠触发 → 后端必须容忍**多次上报**与**长时间断流**；`phone.stale = true` 表示数据超过 15 分钟未更新。

---

## 五、变更记录

| 版本 | 日期 | 改了什么 | 谁 | 兼容性 |
|---|---|---|---|---|
| v0.1 | 2026-09-16 | 初稿：状态枚举、WS 三类消息、手机上报契约 | C | — |

---

## 六、冻结检查清单（W3 周会逐条确认）

- [ ] `pose_state` 6 个枚举值（是否砍掉 `drinking`）
- [ ] `behavior_level` 三档划分
- [ ] WS 是否需要一个"历史数据查询"接口（人机课报告要用）→ 暂定 v0.2 再加
- [ ] 手机上报字段是否需要 `heart_rate`
- [ ] 前端是否需要向后端发消息（v0.1 定为**不需要**）
- [ ] 三人各跑通一次本协议的 mock（后端 mock 推送 / 前端 mock 消费）
