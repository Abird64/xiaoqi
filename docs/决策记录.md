# 决策记录（ADR）

> **写完就不再反复讨论**，除非有新证据。改技术选型必须追加新条目，不许改旧条目。
> 为什么留在 GitHub：① AI 接手任务时要读，避免把已定的事再议一遍；② 答辩要讲「当时为什么这么定」，需要版本留痕。
> 进度 / 分工 → **腾讯文档**（人读的不放这里）。

**模板**：

```markdown
### D-00x 决策标题（YYYY-MM-DD）
- **背景**：为什么要做这个决定
- **选项**：A / B / C 各自代价
- **决定**：选了哪个
- **理由**：一句话
- **影响**：谁要跟着改什么
- **可逆性**：可逆 / 不可逆（不可逆的必须四人一致同意）
- **参与人**：谁在场
```

---

## D-001 前端采用 Three.js 3D 路线，2.5D 作为降级预案（2026-09-06）

- **背景**：房间世界是多媒体课的核心卖点，但 3D 美术与性能都有风险。
- **选项**：A 纯 2.5D 等距精灵（稳但作品感弱）｜B Three.js 3D（上限高、风险高）｜C 先 2.5D 再升级
- **决定**：**B**，并保留 A 作为降级预案。
- **理由**：作品感是这门课的核心评分点；降级预案保证最坏情况仍可交付。
- **影响**：前端工作量集中在 B 身上；W10 设降级触发器。
- **可逆性**：可逆（状态驱动内核不变，换渲染层不影响后端）。
- **参与人**：三人 + 教师方向确认。

## D-002 手机端数据「协议优先，客户端零开发」（2026-09-14）

- **背景**：老师建议加入手机数据（步数/静止），但自研 App 工期不够。
- **选项**：A 自研 App 对接 HealthKit / Health Connect｜B 系统现成工具（快捷指令 / HTTP Shortcuts）+ 后端暴露带令牌接口｜C 不做手机端
- **决定**：**B**。电脑端提供 `POST /api/ingest/health`，手机侧零代码接入。
- **理由**：手机只作为一路**数据源**，不是客户端；省掉开发、审核、上架。
- **影响**：后端承担令牌校验与幂等去重；前端只消费聚合后的步数。
- **可逆性**：可逆（接口不变，换数据来源而已）。
- **参与人**：三人。

## D-003 技术文档放代码仓，人读文档放协作平台（2026-09-16 立，2026-09-22 由 D-006 收窄）

- **背景**：3 人只有 1 人维护个人知识库，队友看不到；口头约定容易漂移。
- **决定**：技术文档与代码同仓。**个人知识库与代码仓彻底分离**（硬规则）。
- **参与人**：三人。
- **后续**：2026-09-22 的 D-006 进一步收窄为「GitHub 只留技术，人读的全进腾讯文档」。

## D-004（保留编号，未使用）

## D-005（保留编号，未使用）

## D-006 团队 3 → 4 人；GitHub 只留技术层，人读文档全进腾讯文档（2026-09-22）

- **背景**：实际团队 4 人，原全套文档按 3 人设计；在线表格比 GitHub markdown 更直观、对没用过 agent 的队员门槛更低；且个人知识库与代码必须分离。
- **选项**：A 全留 GitHub｜B 全迁腾讯（技术文档会和代码分家）｜C **按读者切分**
- **决定**：**C**，判据只有一条：
  > **AI 写代码时要读的 → GitHub；人开会时要读的 → 腾讯文档。**
- **GitHub 保留 6 项**：`README.md`、`系统架构.md`、`接口协议.md`、`需求规格说明书.md`、`本文件`、`assets/`。
  （`需求分析.md`、`UML建模.md` 是实训课交付物 D1/D2，一并留。）
- **迁出腾讯**：看板、分工、协作规则、Git 工作流细节、路线图、里程碑周次、周报、交付清单、模板。
- **理由**：接口与决策必须和代码在同一个 diff 里被评审；进度状态高频更新、要 @ 人、手机可看，表格更合适。
- **影响**：删除 `docs/00-协作/`、`01-规划/`、`03-进度/`、`04-交付物/`、`99-模板/`（git 历史保留）；知识库 `01-Projects/小栖/` 中与技术重叠的规划内容改为指针。
- **可逆性**：可逆（技术层始终在 GitHub）。
- **参与人**：张鹏（提出并拍板），待追认。

## D-007 目录结构 + 单进程进程模型 + 交付形态（2026-09-22）

- **背景**：待决策 P1 从 W3 拖到 W4 仍未定，阻塞编码开工。
- **选项**：A 多进程（采集各自独立进程）｜B 单进程多线程 + asyncio 事件总线
- **决定**：**B**。目录 `backend/` + `frontend/` + `tools/` + `docs/`；**`python -m xiaoqi` 一条命令起全部**（FastAPI 托管前端构建产物，单端口 8000）。
- **详细规格**：见 [系统架构.md](02-需求与设计/系统架构.md) 第三节（目录树、线程表、开发/交付两种形态）。
- **理由**：M4-5 要求「干净机器从零跑起」，单进程单端口是演示与交付的最短路径；MediaPipe 推理放独立线程、只投队列，不阻塞事件循环；开发期用 Vite proxy 免 CORS。
- **影响**：`config.py` 用 YAML 承载阈值 → 直接服务 M2-2「改配置不改代码」。
- **可逆性**：可逆，但**目录重塑成本高，要改必须追加新 ADR**。
- **参与人**：张鹏（A）+ AI 辅助产出，待 B、D 追认。

## D-008 四人方向重排：A 转前端、C 转后端（2026-09-22）

- **背景**：D-006 时定的四人方向（旧 A 感知后端｜旧 B 房间与角色｜旧 C 数据链路与交付｜旧 D 素材与测试执行）在执行中对不齐各人的实际投入；按「分工不写死，谁闲谁上、谁感兴趣谁做」的原则重新对了一遍方向。
- **选项**：A 维持 D-006 原分工｜B 按实际投入重排
- **决定**：**B**，重排为 —— **A 张鹏 = 房间与角色（前端）**（兼进度把控 + 看板维护 + 交付文档主笔）｜**B 罗应发 = 素材与测试执行**｜**C 邓昭一 = 后端（感知 · 状态 · 服务）**｜**D 吴东轩 = 数据链路与联调**。
- **代号映射**：旧 A → 新 C｜旧 B → 新 A｜旧 C → 新 D｜旧 D → 新 B。**只换代号、不改职责归属**。
- **理由**：方向只是大致分工、不是围墙；按各人实际投入与兴趣分配，避免名义分工与实际产出脱节。
- **影响**：
  - `02-需求与设计/系统架构.md`：分层图、三条数据链路主责、模块负责人表（W1–W13）全部按上述映射重排；W13「文档与交付物」主责归 **A**（交付物由 A 全权负责，队友配合执行）。
  - `docs/README.md`：术语速查、降级触发器「谁决定」同步更新。
  - **分工明细与真实姓名以腾讯文档为准**（此处只留代号，避免两处漂移）。
- **可逆性**：可逆（仅文档层，代码与接口未受影响）。
- **参与人**：四人（张鹏整理落文档）。

---

## 待决策清单

| # | 待决策 | 最晚决定时间 | 谁提 |
|---|---|---|---|
| P1 | ✅ 已定 → **D-007** | — | A |
| P2 | 成块开发的固定时间 | W4 内，四人对时 | 全员 |
| P3 | 美术素材来源：自建模型 or 现成 CC0 low-poly | W5 | B |
| P4 | 是否引入 Git LFS（取决于素材体积） | W6 | B |
| P5 | 应用使用记录保留程度（是否长期存库） | W6 | C |
| P6 | 仓库 License（公开仓库建议补一个） | W6 | 全员 |
| P7 | `pose_state` 是否砍 `drinking`、是否随 v0.1 冻 `GET /api/history` | 接口冻结会 | 四人 |
